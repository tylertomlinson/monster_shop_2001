<h1 align= "center">Create Review for <%= link_to @item.name, "/items/#{@item.id}" %></h1>

<%= render partial: 'partials/review_form', locals: {path: "/items/#{@item.id}/reviews", method: :post, button_text: "Create Review"} %>