<h1>Create New Item For <%=link_to @merchant.name, "/merchants/#{@merchant.id}" %></h1>

<%= render partial: 'partials/item_form', locals: {path: "/merchants/#{@merchant.id}/items", method: :post, button_text: "Create Item"} %>
